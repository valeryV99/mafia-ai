const isDev = process.env.NODE_ENV !== 'production'

export const GAME_CONSTANTS = {
  MIN_PLAYERS: isDev ? 1 : 4,
  MAX_PLAYERS: 10,
  NIGHT_MAFIA_TIMEOUT: isDev ? 45_000 : 90_000,
  NIGHT_DETECTIVE_TIMEOUT: isDev ? 20_000 : 30_000,
  NIGHT_DOCTOR_TIMEOUT: isDev ? 20_000 : 30_000,
  DAY_SPEECH_TIMEOUT: isDev ? 80_000 : 120_000,
  DAY_MIN_DURATION: isDev ? 15_000 : 15_000,
  VOTING_TIMEOUT: isDev ? 40_000 : 60_000,
  ROLE_REVEAL_DELAY: isDev ? 3_000 : 5_000,
}
