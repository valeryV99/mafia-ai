const isDev = process.env.NODE_ENV !== 'production'

export const GAME_CONSTANTS = {
  MIN_PLAYERS: isDev ? 1 : 4,
  MAX_PLAYERS: 10,
  NIGHT_MAFIA_TIMEOUT: isDev ? 20_000 : 45_000,
  NIGHT_DETECTIVE_TIMEOUT: isDev ? 12_000 : 25_000,
  NIGHT_DOCTOR_TIMEOUT: isDev ? 12_000 : 25_000,
  DAY_SPEECH_TIMEOUT: isDev ? 40_000 : 80_000,
  DAY_MIN_DURATION: isDev ? 10_000 : 15_000,
  VOTING_TIMEOUT: isDev ? 20_000 : 40_000,
  ROLE_REVEAL_DELAY: isDev ? 2_000 : 4_000,
}
